<?php

if(!function_exists("wf_pow")) {
    function wf_pow($base, $exp) {
        if($base != 0) {
            $value = pow($base, $exp);
        } else {
            $value = pow($base, $exp);
        }
 
        return $value;
    }
}

?>